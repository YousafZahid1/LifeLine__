
import SwiftUI

struct call911: View {

    var body: some View {
        NavigationStack{
            
            ZStack{
                Color.black
                    .ignoresSafeArea()
                Spacer()
                
                VStack {
                    Spacer()
                    Text("LifeLine -AI")
                        .foregroundColor(.blue)
                        .font(.largeTitle)
                        .fontDesign(.rounded)
                    Spacer()
                    Spacer()
                    Spacer()
                    
                   
                    
                    
                    
                }
                .padding()
            }
        }

        
    }
}

#Preview {
    ContentView()
}
